# Prompt — Implementar um slice aprovado

Implemente somente `<SLICE_ID>` conforme o plano aprovado nesta conversa.

Regras:
- mantenha o diff focado;
- não amplie escopo;
- use migrations reversíveis;
- preserve compatibilidade com contratos existentes;
- adicione testes e documentação no mesmo diff;
- crie `docs/traceability/<SLICE_ID>.md`;
- execute lint, typecheck, testes e validações do domínio;
- não faça commit, push ou deploy sem solicitação explícita;
- ao final, apresente arquivos alterados, testes executados, riscos restantes e passos manuais.
