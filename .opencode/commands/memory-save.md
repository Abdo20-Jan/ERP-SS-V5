---
description: Atualiza a memória durável após uma sessão ou checkpoint
agent: memory_curator
subtask: true
---

Atualize o vault `@erp-memory` com base na situação atual e nesta instrução:

$ARGUMENTS

Antes de escrever, verifique o Git e as evidências disponíveis. Registre:

- macro-slice e work packet;
- branch, worktree e commit;
- alterações verificadas;
- testes executados e seus resultados;
- decisões aprovadas;
- riscos e bloqueios;
- próxima ação exata.

Atualize `CURRENT-STATE.md`, crie uma nota de sessão e atualize os índices
correspondentes. Não altere nenhum arquivo fora do vault.
