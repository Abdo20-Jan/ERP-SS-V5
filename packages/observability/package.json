{
  "name": "@sunset/observability",
  "version": "0.0.0",
  "private": true,
  "description": "Observability primitives for Sunset ERP - logging, correlation, metrics",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "lint": "biome lint .",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "pino": "^9.6.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.10.5",
    "pino-pretty": "^13.0.0",
    "typescript": "^5.7.2",
    "vitest": "^3.0.2"
  }
}
