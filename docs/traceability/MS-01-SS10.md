# MS-01-SS10 — Invariantes CI (D-003.12–16)

## Slice
- **ID:** MS-01-SS10
- **Branch:** `codex/ms-01-kernel`
- **Decisões:** D-003.12–16; D-009 commit/push
- **Objetivo:** Scripts read-only que validam invariantes no banco e falham o CI se violadas. Fecha o MS-01.

## Escopo
### Dentro
- `scripts/validate-invariants.ts` (+ módulos 12–16)
- `pnpm run validate:invariants`
- CI integration: migrate → seed → generate → **validate:invariants** → tests
- Smoke test `packages/db/tests/validate-invariants.spec.ts`

### Fora
- domain / prisma schema / migrations / seed / contracts / api / ui
- Implementação real de estoque (D-003.14 skeleton → MS-02)

## Invariantes

| Código | Script | Validação |
|---|---|---|
| D-003.12 | `validate-currency.ts` | Moeda funcional ARS; origin FX com amount+rate; empty DB OK |
| D-003.13 | `validate-double-entry.ts` | Σ débito = Σ crédito em POSTED/REVERSED; XOR linha |
| D-003.14 | `validate-stock.ts` | Skeleton (sempre passa; MS-02) |
| D-003.15 | `validate-state-guards.ts` | Máquina DRAFT/POSTED/REVERSED alinhada ao domínio SS7 |
| D-003.16 | `validate-rbac-audit.ts` | Permissions seed + `validate-guards.py` + contagem audit |

### Semântica de reversal (D-003.15) — alinhada ao domínio
- Original: POSTED → REVERSED (mantém `posted_at`, ganha `reversed_at`)
- Documento de estorno: POSTED com `reversed_from_id` → original REVERSED
- **Não** exige `reversed_from_id` no original REVERSED (prompt genérico estava invertido)

## Como verificar
```bash
# requer DATABASE_URL + migrate/seed locais
pnpm run validate:invariants
# ✅ All invariants passed ; exit 0

python3 scripts/validate-guards.py   # usado por D-003.16
pnpm --filter @sunset/db test        # inclui smoke SS10
```

## CI
Job **Integration Tests** após seed:
1. `prisma generate`
2. `pnpm run validate:invariants`
3. `pnpm test:ci`

## Rollback
- `git revert` do commit SS10
- Scripts são read-only no banco

## Fontes
- D-003.12–16; SS7 ledger semantics; SS9 verify-guards
- Prompt MS-01-SS10; schema `journal_entries` / `journal_entry_lines` / `currencies`
